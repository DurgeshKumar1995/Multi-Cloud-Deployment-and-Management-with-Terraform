# Copy to a file ending in .env and keep it outside Git.
# URL-encode special characters in the password before constructing the URI.
MONGODB_URI=mongodb+srv://YOUR_DATABASE_USER:YOUR_URL_ENCODED_PASSWORD@YOUR_CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DATABASE=multicloud_demo
